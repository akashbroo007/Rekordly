{
  "name": "@rekordly/plugin-youtube",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@rekordly/plugin-sdk": "workspace:*"
  }
}
