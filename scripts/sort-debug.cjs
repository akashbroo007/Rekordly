/* Debug: verify library sort clause generates correct SQL + results */
const path = require('node:path');
process.env.NODE_ENV = 'test';

async function main() {
  // use tsx via pnpm exec below
}
main();